@model IEnumerable<WebStore.Models.Product>;

<div>
    <table class="table">
        <tr>
            <td>
                <p>Name</p>
            </td>

            <td>
            </td>
        </tr>

        @foreach (var product in Model)
        {
            <tr>
                <td>
                    <p>@product.Name</p>
                </td>

                <td>
                    <form method="get" asp-route-id="@product.Id" asp-action="EditProduct">
                        <input class="btn btn-sm btn-primary" type="submit" value="Edit" />
                    </form>
                </td>
            </tr>
        }
    </table>
</div>